1. Prerequisites:

You'll need to have these libraries to build Cruel Hessian:

 libsdl >= 1.2.13
 libsdl-image >= 1.2.6
 libsdl-mixer >= 1.2.8
 libsdl-ttf >= 2.0.9
 cegui >= 0.6.0
 boost_filesystem >= 1.33.0
 boost_regex >= 1.33.0


2. Installation

Check INSTALL file
